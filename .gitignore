node_modules/
dist/
.env
logs/*.jsonl
# transformers.js downloads model weights here on first run
.cache/
# Qdrant local storage
.qdrant_storage/
# Web UI per-repo settings (local state)
data/
# Eval "latest run" pointer (overwritten every run); named snapshots are tracked
eval/results/*-latest.json
# Local-only working docs, kept off the published repo
CLAUDE.md
docs/superpowers/
docs/learning/
